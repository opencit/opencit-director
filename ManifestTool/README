Pre-requisite
1. Create the directory /root/manifest_files
# mkdir /root/manifest_files

2. Provide the necessary values in resources/config.properties file

3. Build the application using command
# ant -f build.xml dist

4. Run the application with root privilege as below
# java -jar dist/lib/ManifestToolApp.jar

